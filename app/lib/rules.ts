/**
 * RULES — 筑巢考研工作台规则引擎
 *
 * 所有魔法常量、扣减规则、判断逻辑统一在此定义。
 * 组件中禁止直接使用硬编码数字，必须引用此文件。
 */

// ============================================================
// 卡片复习间隔 (间隔重复算法)
// ============================================================
export const CARD_REVIEW_INTERVALS: Record<string, number> = {
  "不会": 1,
  "模糊": 3,
  "认识": 7,
  "熟练": 14,
  "稳定": 30,
};

export const CARD_REVIEW_LABELS: Record<string, string> = {
  "不会": "明天",
  "模糊": "3 天后",
  "认识": "7 天后",
  "熟练": "14 天后",
  "稳定": "30 天后",
};

// ============================================================
// 掌握度扣减与增益规则
// ============================================================
export const MASTERY = {
  /** 确认图谱更新时的扣减 */
  confirmUpdatePenalty: 5,
  /** 正确率低于阈值时的扣减 */
  lowAccuracyPenalty: 8,
  /** 掌握度等级扣减 */
  masteryLevelPenalty: 1,
  /** 任务完成后掌握度增益 */
  taskCompletionGain: 20,
  /** 正确率阈值（低于此值触发扣减） */
  accuracyThreshold: 60,
} as const;

// ============================================================
// 任务规则
// ============================================================
export const TASK = {
  /** 备用任务最大分钟数 */
  backupMaxMinutes: 30,
  /** 默认计划生成的任务时长（分钟） */
  defaultPlanMinutes: 60,
  /** AI 推荐最多复习卡片数 */
  maxReviewCards: 10,
  /** 学习计时最小分钟数 */
  minElapsedMinutes: 1,
} as const;

// ============================================================
// 热力图颜色级别 (黑白灰)
// ============================================================
export const HEATMAP = {
  levels: [
    { min: 0, className: "bg-[#F1F1F3]" },
    { min: 1, className: "bg-[#D4D4D8]" },
    { min: 2, className: "bg-[#A1A1AA]" },
    { min: 3, className: "bg-[#71717A]" },
    { min: 4, className: "bg-[#27272A]" },
  ],
  /** 默认最大完成数（达到此值显示最深色） */
  maxCompleted: 4,
} as const;

// ============================================================
// 热力图 cell 与滚动条尺寸
// ============================================================
export const HEATMAP_SIZE = {
  cellSize: 12,
  cellGap: 2,
  labelWidth: 26,
  labelGap: 3,
  scrollbarColor: "rgba(161,161,170,0.5)",
} as const;

// ============================================================
// 进度计算权重
// ============================================================
export const PROGRESS_WEIGHT = {
  mastery: 0.55,
  questions: 0.25,
  resources: 0.20,
} as const;

// ============================================================
// Toast 通知显示时长 (ms)
// ============================================================
export const TOAST_DURATION = 3000;

// ============================================================
// localStorage 相关
// ============================================================
export const STORAGE = {
  key: "nest-exam-workspace-v3",
  /** 最多保留的 studyDays 记录数 */
  maxStudyDays: 140,
} as const;

// ============================================================
// studyDay 记录 slice 上限
// ============================================================
export const MAX_STUDY_DAYS = 140;

// ============================================================
// 日期范围计算最大天数
// ============================================================
export const MAX_DATE_RANGE_DAYS = 900;

// ============================================================
// 热力图 Tooltip 尺寸
// ============================================================
export const TOOLTIP = {
  width: 190,
  height: 56,
  padding: 8,
} as const;

// ============================================================
// chat 保留最近消息数
// ============================================================
export const CHAT_KEEP_LAST = 7;