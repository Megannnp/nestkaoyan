# ═══════════════════════════════════════════════════
# 筑巢考研工作台 · 环境变量模板
# 复制为 .dev.vars（本地开发）或填入 Cloudflare Worker 机密（生产）
#   本地开发：  cp .env.example .dev.vars
#   生产部署：  wrangler secret put DEEPSEEK_API_KEY
# 本文件不含任何真实密钥，可安全提交到 Git。
# ═══════════════════════════════════════════════════

# ─── AI 接口（服务端密钥，绝不下发前端）────────────────
# 真题分析 / 错因分析 / 计划生成 / AI 对话 共用一个模型网关密钥
# 支持任意 OpenAI 兼容网关（DeepSeek / 通义千问 / Kimi / Ollama 本地模型等）
# 也可以在「设置 → AI 学习助手」直接填写 URL + Key（页面配置优先，无需改这里）
DEEPSEEK_API_KEY=
# OpenAI 兼容网关地址（可选；留空默认 https://api.deepseek.com/chat/completions）
# AI_BASE_URL=https://api.deepseek.com/chat/completions
# 默认模型名（可选；留空默认 deepseek-chat；Ollama 本地如 llama3）
# AI_MODEL=deepseek-chat

# ─── 云数据库（可选，默认不启用）──────────────────────
# 当前版本数据保存在浏览器 localStorage（单机、免运维）。
# 需要多端同步/后台统一管理时，启用 Cloudflare D1：
#   1. 在 .openai/hosting.json 设置  "d1": "你的D1数据库名"
#   2. 执行  npm run db:generate && npx wrangler d1 migrations apply <库名>
# 以下为 D1 相关预留变量（启用 D1 时按需取消注释）：
# D1_DATABASE_ID=
# D1_DATABASE_NAME=

# ─── 归属信息（可选，用于页面展示）────────────────────
# 品牌名 / 单位名，留空则显示默认「筑巢考研」
# BRAND_NAME=